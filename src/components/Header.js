import React from 'react'

const Header=({title})=>{
    return (
            <div className="header">
                <h4 className="title">{title}</h4>
            </div>
    );
}

export default Header;