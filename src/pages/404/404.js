import React from 'react'
import { Container } from 'reactstrap';

const NotFound=()=>{
    return(
        <Container fluid>
            <h4 style={{textAlign:"center",marginTop:"1rem"}}>Sinto Muito a pagina que está procurando não existe!</h4>
        </Container>
    );
}

export default NotFound;